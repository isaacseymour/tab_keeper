require 'spec_helper'

describe TabKeeper do
  it 'has a version number' do
    expect(TabKeeper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
